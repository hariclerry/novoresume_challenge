import React, { Component, Fragment } from 'react';

export default class Home extends Component {
    render() {
        return (
            <Fragment>
                {/* Hint: An example usage of a button */}
                {/* <div onClick={this.props.showModal}>
                        <span>Register Today</span>
                    </div> */}
            </Fragment>
        );
    }
}
